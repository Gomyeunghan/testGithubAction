# .github/workflows/actions.yml

# 이름 짓기 워크플로우의 이름으로 추후 깃헙 레포에 표시됩
name: Github Actions 연습하기
# 언제 이 작업을 실행할지 정의
on: push
# 언제 이작업을 실행할건데 (여기서는 push때 마다 실행)

jobs:
  push-job:
    runs-on: ubuntu-latest
    steps:
      - name: 첫 번째 작업
        run: echo "첫 번째 작업 실행 중"
